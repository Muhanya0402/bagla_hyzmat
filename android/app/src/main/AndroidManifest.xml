<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Storage / media — только нужное -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <!-- Android 14+: пользователь может дать доступ не ко всей галерее, а к
         выбранным фото. photo_manager на API 34+ ВСЕГДА запрашивает это
         разрешение вместе с READ_MEDIA_IMAGES (PermissionDelegate34), а
         Android отклоняет незаявленные разрешения молча, даже не показывая
         диалог. Без этой строки частичный доступ выдать было невозможно: в
         системных настройках значилось «Фото: разрешено (выбранные)», а
         приложение видело «нет доступа» и отправляло в те же настройки.
         READ_MEDIA_VIDEO намеренно НЕ добавляем — видео приложению не нужно,
         плагин работает и без него. -->
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />
    <!-- READ_MEDIA_VIDEO УБРАНО: приложение не работает с видео,
         только selfie + photo заказа. Лишний permission уменьшал
         доверие пользователя и попадал под GDPR data minimization. -->

    <application
        android:label="Bagla"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon"
        android:allowBackup="false"
        android:fullBackupContent="false">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
              
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_icon"
            android:resource="@drawable/ic_notification" />
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_color"
            android:resource="@color/notification_color" />
        <!-- Канал для уведомлений, которые рисует система, пока приложение
             свёрнуто или закрыто. Без него Android кладёт их в запасной
             канал Firebase с обычной важностью: без баннера и звука.
             Сам канал создаёт приложение при запуске. -->
        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="bagla_channel" />    

            
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        
        <intent>
            <action android:name="android.intent.action.DIAL" />
            <data android:scheme="tel" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="tel" />
        </intent>
    </queries>
</manifest>